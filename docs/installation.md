# Installation Guide

## Requirements
- Python 3.10+
- Flask
- WireGuard installed
- Nginx (optional, for reverse proxy)

## Steps
1. Clone the repository
2. Run `scripts/setup.sh`
3. Configure `SERVER_PUBLIC_ENDPOINT` in `app.py`
4. Start Nginx if using a reverse proxy
